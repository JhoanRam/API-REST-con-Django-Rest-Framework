# API-REST-con-Django-Rest-Framework
API creada con Python, Django y Django REST Framework, conectada a PostgreSQL y probada con Thunder Client. Incluye entorno virtual y estructura con authproject (configuración) y authapp (lógica de la API). Permite operaciones básicas como listar, crear, actualizar y consultar por ID.
